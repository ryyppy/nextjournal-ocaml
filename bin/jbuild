(jbuild_version 1)

(executables
 ((names (server))
 (public_names (server))
 (modes (byte))
 (libraries (compiler-libs.toplevel findlib.top compiler-libs.common unix yojson reason))
 (flags (:standard -short-paths))
 (ocamlc_flags -custom)))
